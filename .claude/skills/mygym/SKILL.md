---
name: mygym
description: Contexto acumulado del proyecto MyGym — stack, decisiones, convenciones y aprendizajes. Cargar al trabajar en apps/mygym.
---

# MyGym — Contexto del proyecto

Skill viva: se va completando a medida que trabajamos en el proyecto. No repetir acá lo que ya es obvio leyendo el código — solo lo que no es evidente a simple vista.

## Stack (verificado 2026-09-03)

- Frontend: Angular 22.1 (standalone components), generado con Angular CLI 22.1.7
- Ionic 9 + Capacitor 8 — app híbrida (web + móvil nativo)
- Testing: Vitest (`ng test`), jsdom como entorno DOM
- Estilos: SCSS
- HTTP: `HttpClient` de Angular. `HealthService` pega a `${environment.apiUrl}/actuator/health` — el backend expone Spring Boot Actuator, por lo tanto es un backend Java/Spring
- Package manager: npm 11.16.0
- Estructura hasta ahora: `src/app/core/services/` para servicios transversales, `src/app/pages/` para páginas (empezó con `home`)

## Decisiones y convenciones

- (2026-09-03) Multi-tenancy: shared schema + columna `gym_id` en cada tabla tenant-scoped (no schema-per-tenant ni DB-per-tenant). En el backend, `gym_id` es un `Long` plano (no `@ManyToOne`) — patrón a replicar en todo dominio futuro (usuarios, matrículas, pagos).
- (2026-09-03) ~~DB: H2 en memoria~~ **Reemplazado el mismo día por Postgres real en Supabase**, en todos los entornos (no hay perfil H2 de respaldo). Credenciales vía variables de entorno `SUPABASE_DB_URL` (formato JDBC: `jdbc:postgresql://<host>:5432/<db>`, sin usuario/password embebidos), `SUPABASE_DB_USER`, `SUPABASE_DB_PASSWORD` — nunca hardcodeadas en `application.yml` ni commiteadas. `ddl-auto: validate` — Flyway sigue siendo la única fuente de verdad del schema; las migraciones ya estaban en SQL portable, corrieron sin cambios contra Postgres real.
  **Consecuencia importante:** los tests backend (`GymControllerTest` con `@SpringBootTest`, sin `@Transactional`) ahora corren contra la Supabase real, no contra una DB descartable — pueden dejar filas residuales. `GymRepositoryTest` (`@DataJpaTest`) sí hace rollback automático. Si se corren tests seguido, evaluar agregar `@Transactional` a `GymControllerTest` o volver a un perfil de test aislado.
  **Para correr el backend localmente ahora es obligatorio tener seteadas esas 3 variables de entorno** — sin ellas el arranque falla al no poder resolver el datasource.
- (2026-09-03) Backend: paquetes planos por capa (`models`, `services`, `repositories`, `controllers`) + subpaquetes `models.dto` (records) y `services.exception`. Sin auth/Security todavía — módulo futuro separado.
- (2026-09-03) Frontend: páginas nuevas anidadas bajo `src/app/pages/<dominio>/` (ej. `pages/admin/gyms/`), no se introdujo `features/`. Un solo componente sirve para crear y editar cuando el flujo lo permite (ruta con `id` opcional).
- (2026-09-03) Landing pública de marketing en la ruta raíz (`pages/landing/`), montada donde antes vivía el scaffold de health-check (movido a `/status`). Tokens de marca (paleta, fuentes) como CSS custom properties globales en `styles.scss`, mapeados también a las variables de Ionic (`--ion-color-primary`, etc.) para que toda la app herede la identidad. Fuentes self-hosted vía npm (`@fontsource/*`), no Google Fonts por link tag.
- (2026-09-04) **Paleta de marca: "Teal Charcoal + Electric Cyan"** ~~acento cián `#06aebd`~~ **vuelta a lima el mismo día a pedido del usuario** ("volvamos al color lima... manteniendo el color del fondo") → paleta final **"Teal Charcoal + Kinetic Lime"**: fondo `#1e2c30` (carbón con tinte teal, sin cambios), acento único `--brand-accent: #c6ff3d` (lima), `--brand-accent-contrast: #1a2b00`, texto `#eaf6f7`/`#a9c2c6`. El fondo carbón medio se mantuvo porque el claro hacía que las fotos oscuras del gimnasio se vieran "pegadas" en vez de integradas. Tipografía: **Barlow Condensed** (headings) + **Barlow** (body/UI) — sugerencia de `ui-ux-pro-max` por asociación sport/fitness/atlético; Space Mono para cifras (precio, hora, cupos). Cualquier color/gradiente hardcodeado en rgba (no usando los tokens `--brand-*`) queda desincronizado si se vuelve a tocar la paleta — vigilar que no se agreguen nuevos.
- (2026-09-04) **Logo/ícono de marca definido**: mark "badge redondeado + M estilo bumper-plate hexagonal" (generado con la skill `logo-designer`, iteración final tras descartar circles lisos y versión ring/hole por no leerse como "pesas reales"). Fuente en `logos/export/logo.svg` (mismo SVG inline reutilizado en 3 lugares del landing: ícono de nav ~28px, divisor de marca entre FAQ y footer ~96-140px a opacity 0.16, ícono de footer 48px). Favicon del proyecto (`app/public/favicon.svg` + `favicon.ico` multi-tamaño + `apple-touch-icon.png`) generado desde el mismo SVG. Herramientas de export del logo-designer skill (`export.sh`) fallan en este entorno porque referencian paquetes npm inexistentes (`@aspect-build/resvg`, `@resvg/resvg-cli`); usar en su lugar `npx --yes sharp-cli -i logo.svg -o out.png resize W H` para PNGs y `npx --yes png-to-ico a.png b.png c.png > favicon.ico` para el .ico multi-resolución (ambos funcionan sin instalar nada globalmente).
- (2026-09-04) **Textura de fondo (grano sutil) en toda la landing** vía SVG `feTurbulence`+`feColorMatrix` (blanco, alfa máx ~0.05) como data-URI, inyectada en el `--background` de `.landing-content` (`ion-content`) combinada con el color: `--background: url("data:image/svg+xml;base64,...") repeat, var(--brand-ink);`. **Gotcha de Ionic:** `ion-content` NO pinta `--background` en `.inner-scroll` ni en el host — lo aplica en un div interno del shadow DOM `#background-content` (`part="background"`). Cualquier intento de texturizar el fondo con un `::before`/regla CSS directa sobre `.landing-content` o `.inner-scroll` no se ve; hay que pasar el valor completo (imagen + color) a través de la custom property `--background`, que Ionic sí reenvía a ese div interno. Verificar con `getComputedStyle(ionContentEl.shadowRoot.querySelector('#background-content'))`.
- (2026-09-03) **Build de deploy separado solo para la landing** (`ng build landing`), pensado para publicar en un dominio público sin exponer `/admin/gyms` (que todavía no tiene auth). Es un segundo `project` en `angular.json` (`main.landing.ts` bootstrapea el componente `Landing` directo, sin `app.routes.ts` ni Router) → `dist/landing/browser`, sin ningún chunk de admin. `Landing` no depende de Router (nav interna es por anchors `#hash`), por eso esto funciona sin duplicar código. Verificado sirviendo el build está con `http-server`: `/admin/gyms` y `/status` dan 404 (no existen en este bundle).
  **Cómo mantener esto al tocar `Landing`:** si en algún momento se le agrega routing real o dependencias de `HttpClient`/backend, hay que revisar `main.landing.ts`/`app.config.ts` para que el build de landing siga sin arrastrar cosas del admin.
- (2026-09-04) **El formulario de contacto de la landing envía el mail de verdad vía una función serverless de Vercel**, no solo `mailto:` (que dependía de que el visitante tuviera un cliente de correo configurado — en la práctica muchas veces no pasaba nada visible). Estructura nueva: `app/vercel-landing/` (fuera de `dist/`, no se pisa con cada build) contiene `vercel.json` (`outputDirectory: "public"`), `api/contact.js` (función Node que llama a la API de Resend) y `public/` (el build de `dist/landing/browser` copiado ahí antes de cada deploy — gitignoreado, hay que regenerarlo). El deploy ahora se hace `cd vercel-landing && vercel --prod ...` en vez de deployar directo desde `dist/landing/browser`.
  **Requiere** la env var `RESEND_API_KEY` cargada en el proyecto de Vercel (dashboard → Settings → Environment Variables, nunca pegada en chat). El remitente usa `onboarding@resend.dev` (sandbox de Resend, no requiere verificar dominio) — si se quiere mandar desde `@cortesdev.cl` hay que verificar ese dominio en Resend (registros DNS en NIC.cl, mismo mecanismo que la config del dominio propio).
  **El frontend** (`landing.ts`) hace `fetch('/api/contact', ...)` y maneja tres estados con signals: `isSending`, `isSubmitted`, `submitError` — si el fetch falla (offline, función no desplegada, Resend caído), el modal muestra un mensaje de error con los datos del form intactos para reintentar, sin perder lo que el usuario escribió. WhatsApp sigue como canal alternativo siempre visible arriba del form.
  **Gotcha de testing:** `ng serve` local no sirve `/api/contact` (esa función solo existe en el deploy de Vercel) — el fetch siempre falla en local y cae al estado de error. Es el comportamiento esperado, no un bug; para probar el envío real hay que estar en el deploy de Vercel con `RESEND_API_KEY` configurada.

- (2026-09-04) **Fase 1 de producto (auth Google + socios + reservas) verificada de punta a punta contra Supabase real** (proyecto nuevo `db.meyumlacmvkesdvlzgcq.supabase.co`, usuario `postgres`, base `postgres`) — `mvn test` con `SUPABASE_DB_URL/USER/PASSWORD` + `APP_JWT_SECRET` + `GOOGLE_CLIENT_ID` seteados: 17/17 tests OK, Flyway aplicó V1+V2 sin problemas. **Nuevas env vars obligatorias además de las 3 de Supabase**: `APP_JWT_SECRET` (≥32 caracteres, HS256 para el JWT propio) y `GOOGLE_CLIENT_ID` (Google OAuth Client ID — puede ser cualquier string no vacío para que arranque el contexto de test/local; para que el login real funcione tiene que ser el Client ID real de un proyecto en Google Cloud Console). Sin estas 5 variables el contexto de Spring ni siquiera levanta (ni para tests que no tocan auth), porque `JwtService`/`GoogleTokenVerifier` las resuelven vía `@Value` al construirse.
- (2026-09-07) **Decisión de producto: la app móvil (Capacitor, proyecto `app`) es solo para socios.** Toda la administración (super-admin propio + panel de cada dueño de gimnasio) se movió al proyecto `landing` (el mismo que sirve `mygym.cl`), siempre detrás de login de Google. Detalle:
  - Nuevo rol `SUPER_ADMIN` (antes solo `GYM_ADMIN`/`MEMBER`). Un super-admin no tiene `gym_id` — se relajó la constraint `NOT NULL` de `app_user.gym_id` en `V3__app_user_gym_id_nullable.sql`. `AuthenticatedUser.from(jwt)`/el frontend (`AuthUser.gymId`, `LoginResponse.gymId`) ahora tratan `gymId` como nullable.
  - `/api/gyms/**` (antes `permitAll()`) ahora requiere `hasRole("SUPER_ADMIN")` en `SecurityConfig` — el super-admin, que antes no tenía ningún login, ahora sí. `GymControllerTest` se actualizó para mandar un bearer token en cada request.
  - **Los proyectos `app` y `landing` comparten el mismo árbol `src/`** (mismo `sourceRoot` en `angular.json`, cada uno con su propio `main.*.ts`/`index.*.html`/routes/config) — mover admin "a la web" no movió ni un archivo de página, solo se creó `web.routes.ts`/`web.config.ts` (rutas `login`, `gym-admin`, `admin/gyms/*`, todas con `authGuard`/`roleGuard`) y `main.landing.ts` pasó de bootstrapear el componente `Landing` solo (sin Router) a bootstrapear el shell `App` (`<ion-app><ion-router-outlet>`, el mismo que ya usaba la app móvil) con `webConfig`. `app.routes.ts` (móvil) se achicó a `login`/`member`/`status` únicamente; `''` ahora redirige a `/login` en vez de mostrar la landing de marketing (ya no tiene sentido dentro de la app instalada).
  - El bloque de `SocialLoginModule.initialize(...)` + el workaround de `SOCIAL_AUTH_CONFIG` (ver entrada de abajo sobre el bug de `angularx-social-login`) se extrajo a `core/providers/google-auth.providers.ts` (`provideGoogleAuth()`) para no duplicarlo entre `app.config.ts` y `web.config.ts`.
  - **Pendiente, no resuelto acá:** dónde se despliega `mygym-api` en producción — nunca se desplegó, solo corrió local (`mvn spring-boot:run`). El panel web de admin en `mygym.cl` no va a poder hablarle a ningún backend real hasta que eso se decida y `environment.apiUrl` se actualice. `vercel-landing/vercel.json` ya tiene el rewrite de SPA (`/(.*) → /index.html`) necesario para que las rutas cliente-side no den 404 en producción.
  - Cómo se provisiona un `SUPER_ADMIN` o un `GYM_ADMIN` de prueba: insertar/actualizar directo en `app_user` (no hay autoregistro para ningún rol admin) — ver el script Node ad-hoc con `pg` usado para sembrar datos de demo (sección de aprendizajes más abajo).
- (2026-09-07) **`mygym-api` desplegado en producción: Railway.** Proyecto `mygym-api`, servicio `api`, build automático con Railpack (detecta Maven solo). Dominio: `https://api-production-be95.up.railway.app`. Env vars cargadas ahí: las 3 de Supabase + `APP_JWT_SECRET` + `GOOGLE_CLIENT_ID` (ya con el Client ID real de Google Cloud, proyecto `apruebapyme`). `server.port` pasó a `${PORT:8080}` (Railway inyecta `PORT`, local sigue en 8080 por default). `environment.ts`/`environment.development.ts` del frontend ya apuntan a esta URL y tienen el Client ID real. `mygym.cl` en producción ya funciona de punta a punta (login Google real, `/admin/gyms` y `/gym-admin` con datos reales).
  **Gotcha importante — Supabase direct connection es IPv6-only, Railway no tiene salida IPv6:** el host directo `db.<ref>.supabase.co` da `SocketException: Network is unreachable` desde Railway. Fix: usar el **Session pooler** de Supabase en vez de la conexión directa (Supabase dashboard → Connect → tab "Direct Connection string" → radio "Session pooler", ahí muestra host/puerto/usuario correctos). Cambia tanto el host (`aws-0-<región>.pooler.supabase.com:6543`, no `db.<ref>.supabase.co:5432`) como el usuario (`postgres.<project-ref>`, no `postgres` a secas) — la contraseña es la misma.
  **Segundo gotcha, más grave — el pooler rompe los prepared statements de JDBC.** Con el connection string de arriba, cualquier request a un endpoint que hace SELECT-por-id (`GET /api/gyms/{id}`, etc.) fallaba con 500 en producción: `PSQLException: bind message supplies 1 parameters, but prepared statement "S_8" requires 0`. Causa: el pooler de Supabase (aunque la UI lo llama "Session pooler") enruta las conexiones de forma que el nombre de prepared statement cacheado por el driver de Postgres para una conexión previa choca contra otra distinta del pool — el problema clásico de PgBouncer/Supavisor en modo transacción con drivers JDBC que cachean statements preparados del lado del servidor. **Fix:** agregar `?prepareThreshold=0` al final de `SUPABASE_DB_URL` (`jdbc:postgresql://aws-0-<región>.pooler.supabase.com:6543/postgres?prepareThreshold=0`) — esto le dice al driver que nunca use prepared statements del lado del servidor. Sin este parámetro, cualquier hosting que use el pooler de Supabase (no solo Railway) va a fallar tarde o temprano con este mismo error, y puede pasar desapercibido un rato porque las primeras requests después de un deploy nuevo a veces funcionan (antes de que el pool reutilice conexiones).
  **CORS agregado**: `SecurityConfig` ahora tiene un `CorsConfigurationSource` (antes no hacía falta, todo era same-origin en local). Orígenes permitidos vía `app.cors.allowed-origins` (env var `APP_CORS_ALLOWED_ORIGINS`, default `https://www.mygym.cl,https://mygym.cl,http://localhost:4300`) — agregar cualquier dominio nuevo (previews de Vercel, etc.) ahí si hace falta.
  **Bug real encontrado por el build limpio de Railway, no por los tests locales:** `ReservationService.listOccurrences` capturaba la variable de control de un `for` clásico (`date`, reasignada en el propio for) dentro de un lambda — "local variables referenced from a lambda expression must be final or effectively final". Esto es un error de compilación real (JLS), pero **Maven local nunca lo detectó** porque nunca se corrió `mvn clean` en toda la sesión — la compilación incremental reusaba el `.class` ya compilado de ese archivo (que no se había vuelto a tocar) y nunca lo recompilaba. Lección: correr `mvn clean compile`/`clean test` al menos una vez antes de confiar en que "ya compiló antes" sigue siendo válido, sobre todo antes de un deploy real.
- (2026-09-04) **`mygym.cl`/`www.mygym.cl` resueltos y en producción.** Vercel nunca provisionó una zona DNS propia para el dominio (agregarlo al proyecto/dashboard no crea una zona automáticamente — `vercel dns add` fallaba con "not a DNS zone" aun con NS apuntando a `vercel-dns.com`). Solución final: dominio vive en el mismo hosting cPanel que `cortesdev.cl` (cuenta `cco39814`) — se agregó `mygym.cl` ahí (aunque el panel mostraba "Dominios Adicionales 0/0", el alta funcionó igual), se cambiaron los nameservers en NIC.cl a `ns1.cpanelhost.cl`/`ns2.cpanelhost.cl`, y en Zone Editor de cPanel: A apex → `216.198.79.1` + `64.29.17.1` (los dos que pide Vercel), `www` CNAME → `cname.vercel-dns.com.`. Diagnóstico con `vercel domains verify mygym.cl` (da el estado exacto: `configured-correctly` cuando quedó bien). La propagación del cambio de nameservers a nivel del registro `.cl` tardó varios días.

## Aprendizajes / gotchas

- (2026-09-03) **`proxy.conf.json` con glob `/algo/*` solo matchea UN segmento de path.** `/api/*` proxea `/api/gyms` pero NO `/api/gyms/2` ni `/api/gyms/2/blocks` (caen al fallback SPA de `ng serve`, devolviendo `index.html` con status 200 y `Content-Type: text/html` — un 200 "fantasma" que no es fácil de notar sin mirar el `Content-Type`). Usar `/api/**` para que matchee cualquier profundidad de subrutas. Además, `ng serve` solo lee `proxy.conf.json` al arrancar — hay que reiniciarlo después de editarlo, `ng serve` no hace watch de ese archivo.
- (2026-09-03) **Ionic fija `body`/`ion-app` en `position: fixed; height: 100%` (shell de app nativa) — el documento NUNCA scrollea.** Una página nueva con HTML plano (sin `<ion-content>`) se ve recortada al alto del viewport y no hace scroll aunque tenga contenido debajo: `window.scrollTo`, `document.body.scrollHeight` y un `IntersectionObserver` con `root: null` no fallan con error, simplemente no pasa nada porque el contenedor real no se mueve. Toda página nueva (landing incluida) debe envolver su contenido en `<ion-content>` para heredar el scroll interno de Ionic.
- (2026-09-03) **Consecuencia del punto anterior:** el screenshot `fullPage` de chrome-devtools MCP mide `document.documentElement.scrollHeight` para decidir el alto a capturar — como ese scrollHeight queda fijo al viewport dentro de `ion-app`, un `fullPage` en cualquier página de este proyecto solo trae el hero/primer viewport, aunque haya mucho más contenido debajo. Para verificar una página completa hay que scrollear el `ion-content` real (`await content.getScrollElement()`) por tramos y sacar screenshots normales, no confiar en `fullPage`.
- (2026-09-04) **`@DataJpaTest` sin `@AutoConfigureTestDatabase(replace = Replace.NONE)` falla desde que se sacó H2 del classpath** ("Failed to replace DataSource with an embedded database for tests") — Spring Boot por defecto intenta reemplazar el datasource de un `@DataJpaTest` por uno embebido, y como el proyecto usa Postgres real siempre (sin H2 de respaldo), no hay con qué reemplazarlo. `GymRepositoryTest` tenía este gap desde el cambio a Supabase (nadie había corrido `mvn test` completo hasta ahora); el fix es agregar esa anotación a cualquier `@DataJpaTest` nuevo.
- (2026-09-03) **Nunca ocultar contenido con `opacity: 0` a la espera de que JS (scroll-reveal, IntersectionObserver) lo haga visible.** Si `prefers-reduced-motion` está activo en el SO/navegador, o el JS falla/tarda, el contenido queda invisible para siempre y no hay error en consola que lo delate — se vive como "la página está vacía/pobre". Patrón correcto: contenido visible por defecto (`opacity: 1`), la animación de scroll solo agrega un `transform` sutil encima, nunca gatea la visibilidad.
- (2026-09-08) **`<ion-modal>` con contenido inline (`<ng-template>` + `<ion-header>`/`<ion-content>` como hijos directos, sin wrapper) colapsa el `ion-content` a una altura mínima (~56px) y le aplica `contain:size` + `overflow:hidden` — todo lo que queda debajo del primer campo del form se recorta invisible, aunque el DOM esté perfectamente hidratado (Angular/Ionic no tiran ningún error, `getBoundingClientRect()` de cada campo devuelve valores correctos, `getComputedStyle` también — el bug no se detecta inspeccionando estilos computados, solo mirando el screenshot real).** Causa: sin la clase `.ion-page` en el elemento raíz del contenido, no se aplica la regla `.ion-page{display:flex;flex-direction:column;height:100%}` de `@ionic/angular/css/core.css` que hace que `ion-content` (flex child) se estire para llenar el modal. Fix: envolver el `<ion-header>`+`<ion-content>` del componente de contenido del modal en `<div class="ion-page">...</div>`. Aplica a cualquier modal Ionic-Angular con contenido inline (no a los creados vía `modalController.create()`, que sí agregan la clase solos).
- (2026-09-08) **`vercel-landing/public/` NO se regenera solo — es un paso manual fácil de olvidar.** `ng build landing` escribe en `dist/landing/browser/`, pero Vercel sirve desde `vercel-landing/public/` (`outputDirectory` en `vercel.json`). Si se corre `vercel --prod` sin antes copiar `dist/landing/browser/*` → `vercel-landing/public/`, el deploy "funciona" (Vercel da `READY`, el HTML se actualiza con nuevo `last-modified`) pero sirve los chunks JS **viejos** porque `public/` no cambió — un falso positivo silencioso, nada indica el error salvo comparar el contenido real de los chunks servidos contra el fix esperado. Checklist de deploy de la landing: `ng build landing` → `rm -rf vercel-landing/public/* && cp -r dist/landing/browser/* vercel-landing/public/` → `cd vercel-landing && vercel --prod --yes`.
- (2026-09-08) **Bug de seguridad real (no solo de UI): el panel `/gym-admin` (dueño de gimnasio) llamaba a los mismos endpoints `/api/gyms/{id}` que el panel super-admin**, protegidos con `hasRole("SUPER_ADMIN")` desde el cambio del 2026-09-07 — cualquier `GYM_ADMIN` real recibía 403 en todo (ver gym, listar/crear/editar/eliminar bloques), aunque el login funcionara. El plan original de "mover admin a la web" asumió que `GymController` solo lo usaba el super-admin y no contempló que `GymAdmin` (la página del dueño) reusa el mismo `GymService` Angular. **Fix:** nuevo `GymAdminController` en `/api/gym-admin/gym` (protegido `hasRole("GYM_ADMIN")`) que resuelve `gymId` **desde el JWT del caller**, nunca de un path variable — mismo patrón que ya usaba `MemberController` en `/api/gym-admin/members`. Nunca agregar `GYM_ADMIN` como rol permitido de `/api/gyms/**` directamente: ese controller confía en el `{id}` de la URL sin verificar ownership, así que abriría un IDOR (un dueño podría editar el gimnasio de otro con solo cambiar el id en la URL).
- (2026-09-08) **chrome-devtools MCP puede quedar en `CONNECT_TIMEOUT` para la sesión aunque `claude mcp list` lo muestre "Connected"** (el problema es de la conexión de esta sesión puntual, no del server) — ni reconectar vía `/mcp` ni un `/compact` lo revivieron en un caso real. Workaround que no depende de la MCP: usar Playwright apuntando a un Chrome/Edge ya instalado en el sistema (`chromium.launch({ channel: 'chrome', headless: true })`), evitando la descarga del binario propio de Playwright (que además puede fallar en redes con WARP — ver `user_windows_network_quirks`). Da control real de navegador (DOM, computed styles, screenshots, network) sin la MCP. Para probar paneles con auth (Google OAuth) sin loguearse de verdad: forjar un JWT HS256 con el mismo `APP_JWT_SECRET` de Railway/local (claims `sub`, `userId`, `role`, `gymId`, `iat`, `exp` — ver `JwtService.java`) e inyectarlo en `sessionStorage` (`mygym.auth`, ver `auth.service.ts`) antes de navegar a una ruta protegida.

- (2026-09-08) **Row Level Security (RLS) estaba deshabilitado en las 4 tablas públicas** (`gym`, `gym_block`, `app_user`, `reservation`) — Supabase avisó por mail que cualquiera con la URL del proyecto podía leer/editar/borrar todo vía su API REST auto-generada (PostgREST), que expone cualquier tabla sin RLS a los roles `anon`/`authenticated`. El backend nunca usa esa API (se conecta directo por JDBC como el rol dueño de las tablas, que **bypassea RLS**), así que el fix (`ALTER TABLE ... ENABLE ROW LEVEL SECURITY` sin policies, en `V4__enable_row_level_security.sql`) cierra la API pública sin afectar la app.
  **Gotcha grave que causó una caída real de producción:** intentar incluir `ALTER TABLE flyway_schema_history ENABLE ROW LEVEL SECURITY` en esa misma migración **cuelga el deploy indefinidamente** — Flyway usa esa tabla para su propio locking/bookkeeping mientras la migración corre, así que alterarla desde adentro de una migración que Flyway está ejecutando genera un deadlock real contra el propio Flyway (confirmado con `pg_stat_activity`: una sesión "idle in transaction" corriendo la query interna de Flyway, bloqueando a la sesión que corre el `ALTER`, que a su vez bloquea a Flyway). Railway además reintenta el deploy fallido automáticamente (`restartPolicyMaxRetries`), recreando el mismo deadlock en cada intento — hay que matar las sesiones colgadas en Postgres (`pg_terminate_backend`, vía `pg_stat_activity` filtrando por la query o por `state = 'idle in transaction'`) **y** subir el fix casi simultáneamente, o el loop de reintentos las vuelve a colgar. **Nunca tocar `flyway_schema_history` desde una migración manejada por Flyway** — si hace falta protegerla, dejarla afuera (es solo metadata de versiones/checksums, no datos de la app, bajo riesgo real de exposición).

- (2026-09-08) **Crear un gimnasio ahora también crea a su "dueño" (`GYM_ADMIN`) en la misma transacción.** Decisión de producto: el super-admin solo crea/da de baja/configura gimnasios; cada dueño de gimnasio administra el suyo propio (esto último ya estaba resuelto por el fix de seguridad del mismo día — `GymAdminController` resuelve `gymId` desde el JWT). Lo que faltaba era *cómo* ese dueño llega a existir sin insertar SQL a mano. `GymCreateRequest` ahora exige `ownerName`+`ownerEmail`; `GymService.createGym` valida que el email no esté tomado (`DuplicateOwnerEmailException` → 409, mismo patrón que `DuplicateMemberEmailException`) y crea el `AppUser` (`role=GYM_ADMIN`, `gymId` del gym recién creado, `googleSub=null`) en la misma transacción (clase ya `@Transactional`) — el dueño queda enlazado a su cuenta real de Google recién en su primer login (`AuthService.loginWithGoogle` setea `googleSub` la primera vez que el email coincide, patrón que ya existía). `googleLoginEnabled` del gym pasa a default `true` en creación (antes `false`) para que el dueño pueda entrar de inmediato sin que el super-admin tenga que acordarse de prender el toggle después. `GymResponse` ahora expone `ownerName`/`ownerEmail` (null si el gym no tiene dueño — casos viejos pre-feature) resolviéndolos vía `AppUserRepository.findByGymIdAndRole(gymId, GYM_ADMIN)` en `toResponse`.
- (2026-09-08) **Gotcha de Playwright + Ionic: un click sobre `ion-button[type="submit"]` puede no disparar el submit del form, sin ningún error.** `page.locator(...).click()` (con o sin `force:true`) y hasta un `element.click()` disparado por JS a veces no alcanzan a activar el `<button>` nativo que Ionic esconde dentro del shadow DOM (o el hidden sibling `<button style="display:none">` que usa para el submit nativo) — no tira excepción, simplemente no pasa nada: no hay POST, no hay navegación, no hay error de consola. Diagnosticado agregando listeners de `submit`/`click` antes de clickear. **Fix confiable:** conseguir el `boundingBox()` del locator y usar `page.mouse.click(x, y)` con las coordenadas del centro — un click real a nivel de sistema operativo, no un evento sintético. Aplica en general a cualquier `ion-button[type="submit"]` de este proyecto cuando un test de Playwright parece "no hacer nada" al confirmar un formulario.

- (2026-09-08) **Un gimnasio puede tener varios `GYM_ADMIN`, no solo uno.** El super-admin gestiona la lista completa desde `gym-form` (sección "Administradores"): agregar (`POST /api/gyms/{id}/admins`, valida email único vía `DuplicateOwnerEmailException`), listar (`GET /api/gyms/{id}/admins`) y activar/desactivar (`PUT /api/gyms/{id}/admins/{userId}`, resuelto vía `AppUserRepository.findByIdAndGymId` + chequeo de `role==GYM_ADMIN`, 404 si no matchea — nunca hard-delete, así se conserva el historial y `AuthService.loginWithGoogle` ya bloquea el login de un `active=false` sin cambios extra). El campo `ownerName`/`ownerEmail` de `GymCreateRequest` sigue creando el primer admin en la misma transacción que el gym; no se movió a un flujo separado.
- (2026-09-08) **Personalización por gimnasio: color de acento + logo, ambos pensados para reusarse después en la futura app de socios.** Columnas nuevas en `gym` (`V5__gym_theme_and_logo.sql`): `theme_color` (hex, lo elige el propio `GYM_ADMIN` desde su panel — `PUT /api/gym-admin/gym/theme`, gymId resuelto del JWT) y `logo_svg` (texto, lo carga el super-admin pegando el SVG en `gym-form` → `GymConfigUpdateRequest.logoSvg`, **generado a mano por mí con la skill `logo-designer`, no automatizado** — ese skill es conversacional/interactivo, no algo que el backend pueda invocar solo). El panel `/gym-admin` tiene una pestaña "Personalización" con: el logo (si existe) y 3 swatches de color al azar (de una paleta curada de 7, botón "Ver otras opciones" para resamplear) — al elegir uno, el acento se aplica *solo dentro de ese panel* vía CSS custom properties (`--ion-color-primary`, `--brand-accent`, y sus `-contrast`) bindeadas con `[style.--x]` en `<ion-header>`/`<ion-content>` del propio componente (no toca `:root`, así que no se filtra a otras rutas).
  **Gotcha: `[innerHTML]` con SVG no hereda la encapsulación de Angular.** Una regla `.gym-logo svg { width: ... }` en el SCSS del componente nunca matcheaba el SVG inyectado por `[innerHTML]`, porque Angular solo estampa el atributo `_ngcontent-*` (usado para *scopear* las reglas del componente) a elementos que él mismo crea desde el template — no a markup crudo insertado vía `innerHTML`. Fix: `::ng-deep svg { ... }` dentro del selector padre.
- (2026-09-08) **Gotcha de Playwright: un click en `ion-button[type="submit"]` a veces no dispara nada, ni siquiera con `force:true` o un `.click()` disparado por JS — hay que esperar a que se le saque el atributo `disabled` antes de clickear.** Confirmado repetidas veces: si el click (real, con `mouse.click()` en el centro del `boundingBox()`) se dispara mientras Angular todavía no terminó de recalcular la validez del form (un frame después de `fill()`/`Tab`), el navegador directamente no ejecuta nada porque el botón nativo sigue disabled en ese instante — sin excepción, sin request, sin re-render visible en el screenshot siguiente (a veces ni el estado visual cambia, quedando engañosamente igual a como si el click nunca hubiera pasado). Patrón confiable: antes de calcular el `boundingBox()` y clickear, hacer polling corto (`while (await locator.evaluate(el => el.hasAttribute('disabled'))) await sleep(200)`) hasta confirmar que ya no está disabled.

- (2026-09-08) **Sugerencia de branding (color + logo) con IA usa Google Gemini gratis (API key del propio Gmail del usuario, `https://aistudio.google.com/apikey`), no Anthropic** — decisión explícita del usuario para no requerir una key paga. `BrandingSuggestionService` pide un JSON `{paletteKey, logoSvg}` en una sola pasada (SVG completo generado por el modelo, no un generador determinístico), valida `paletteKey` contra una lista fija de 7 paletas propias (nunca confía en el hex que devuelva el modelo) y sanitiza el SVG contra un denylist (`<script`, `onload=`, etc.) antes de guardarlo — el modelo es un origen no confiable.
  **Gotcha de nombres de modelo:** `gemini-2.0-flash` (el default hardcodeado inicialmente) fue removido — devuelve 404 recomendando `gemini-3.6-flash` en el mismo error. Los nombres de modelo de Gemini rotan sin aviso; si `/suggest-branding` empieza a devolver 502, lo primero a revisar es si el modelo en `GEMINI_MODEL` (env var de Railway) sigue existiendo, no asumir que es un problema de la API key. `gemini-3.6-flash` es un modelo "thinking" (devuelve `thoughtSignature`/`thoughtsTokenCount`) — tarda bastante más que un modelo no-thinking (varios segundos, a veces 15-20s), cualquier test/timeout debe esperar la respuesta real (`page.waitForResponse`) en vez de un `waitForTimeout` fijo corto.

- (2026-09-09) **Email real de invitación a administradores, vía Resend.** Cuando el super-admin crea un gimnasio (dueño inicial) o agrega un administrador adicional, `GymService` llama a `AdminInviteEmailService`, que arma un HTML (tablas + estilos inline, sin fuentes web, compatible con Outlook de escritorio) a partir de la plantilla `api/src/main/resources/templates/email/admin-invite.html` (placeholders simples `{{TOKEN}}`, reemplazados con `String.replace`, sin motor de templating nuevo) y lo envía por la API REST de Resend (`POST https://api.resend.com/emails`, mismo patrón `RestClient` que ya usa `BrandingSuggestionService` para Gemini). El envío es **best-effort**: si falla (key ausente, Resend caído, restricción de sandbox), se loguea y se traga la excepción — el alta del admin en la base nunca debe fallar por un email que no salió.
  El logo del gimnasio se inyecta como **SVG inline** (nunca como `<img src="data:image/svg+xml...">`, que Outlook de escritorio no soporta) — si el gimnasio no tiene `logoSvg` todavía, se usa la primera letra del nombre como reemplazo. Se decidió **no rasterizar a PNG** para esta primera versión (evita agregar una dependencia pesada tipo Batik al backend Java); queda como mejora futura si hace falta más fidelidad en Outlook de escritorio.
  Los 7 pares color/contraste de paleta (antes solo en el frontend, `gym-admin.ts`) se centralizaron también en el backend en `GymPalette.java` — fuente de verdad duplicada a propósito (un archivo por runtime), **si se cambia un color hay que actualizar los dos lugares**.
  **Gotcha real encontrado al probar el envío:** la cuenta de Resend usada está en modo sandbox — solo puede mandar emails a la dirección con la que se registró esa cuenta (`gvegasc@gmail.com`), a cualquier otro destinatario le da 403 (`"You can only send testing emails to your own email address"`). Se prueba fácil: un alias `+` de Gmail (`email+test@gmail.com`) NO cuenta como "tu propio email" para Resend — hay que usar el email exacto. Fix definitivo: verificar un dominio propio en Resend (`mygym.cl`, pendiente — ver memoria `mygym_pending_resend_domain`), después de lo cual cualquier destinatario funciona y sube el límite de envíos.
- (2026-09-09) **`JAVA_HOME` de esta máquina puede apuntar a Java 8 aunque `java` en el PATH sea 21.** `mvn -version` mostró "Java version: 1.8.0_502" mientras `java -version` (resuelto por PATH) mostraba 21 — Maven usa `%JAVA_HOME%`, no el PATH, para encontrar el JDK. Sin darse cuenta, cualquier `mvn compile`/`mvn test` corrido así intenta compilar código con text blocks (`"""..."""`, Java 17+) usando un compilador Java 8, lo que produce una cascada larga y confusa de errores de sintaxis ("class, interface, or enum expected", "unclosed string literal") en archivos que ni se tocaron — no es un error real de código. Fix: exportar `JAVA_HOME` al JDK 21 instalado (`C:\Program Files\Eclipse Adoptium\jdk-21.0.12.8-hotspot` en esta máquina) antes de correr `mvn` desde una sesión/terminal nueva.
- (2026-09-09) **Todo texto visible del producto (UI, emails, prompts a otras IAs) tiene que estar en español latino neutro — cero voseo.** Se coló voseo ("tenés", "podés", "elegí", "agregá", "probá", "pegá", "sos") en la plantilla de email y en varias pantallas (`gym-form.html`, `gym-admin.html`) al escribirlas de una — la regla del usuario (que ya existía para el diálogo del chat) aplica igual de estricto al contenido que se genera para el producto. Antes de dar por cerrada cualquier feature con texto nuevo, revisar explícitamente que no haya voseo/modismos regionales.

- (2026-09-09) **`mygym.cl` verificado como dominio propio en Resend** — el envío de emails de invitación a administradores (ver entrada anterior) ya funciona con cualquier destinatario, no solo con el email de la cuenta de Resend. Se agregaron 4 registros DNS en el Zone Editor de cPanel (cuenta `cco39814`, `https://cortesdev.cl:2083/`, mismo hosting que ya sirve `mygym.cl`): TXT `resend._domainkey` (DKIM), CNAME `rsend`/`send` (SPF/envío), TXT `_dmarc`. `RESEND_FROM` en Railway pasó de `mygym <onboarding@resend.dev>` a `mygym <notificaciones@mygym.cl>` (requiere `railway redeploy`, no alcanza con solo cambiar la env var — Spring lee `@Value` al arrancar).
  **Gotcha: login con Google no funciona en un Chrome lanzado por Playwright** — Google detecta el navegador automatizado y bloquea el login. Para cualquier flujo que necesite loguearse con Google (Resend, y potencialmente otros), hay que pedirle al usuario que abra su Chrome normal y se loguee ahí. Además, si se lanza un Chrome visible con `chromium.launch()` (sin `--remote-debugging-port`) para que el usuario se loguee, no hay forma de reconectarse a esa misma instancia desde otro script después — si se necesita seguir automatizando tras el login, hay que exponer un puerto de debug remoto desde el arranque, o simplemente darle al usuario los valores exactos para que los cargue él mismo en esa ventana ya autenticada (lo que se hizo para cargar los registros DNS en cPanel).

- (2026-09-09) **Bug real reportado por el usuario: agregar un admin con un email duplicado no mostraba ningún error visible.** Causa doble:
  1. **Frontend:** `gym-form.ts` reusaba el signal genérico `status` (compartido entre config, bloques y admins) para marcar el error, y el único bloque `@if (status() === 'error')` del template estaba ubicado entre las cards de "Configuración" y "Bloques/horarios" — lejos del formulario de "Agregar administrador" que en verdad falló. El usuario nunca veía el mensaje sin scrollear. **Fix:** signal dedicado `adminError` (no el `status` global), con su propio `<ion-note color="danger">` pegado justo debajo del botón "Agregar administrador".
  2. **Backend (más importante, patrón a repetir en toda la app):** todos los mensajes de excepción en `services/exception/*.java` (`DuplicateOwnerEmailException`, `GymNotFoundException`, etc.) y los títulos en `ApiExceptionHandler` estaban en **inglés**. El `errorInterceptor` del frontend (`core/interceptors/error.interceptor.ts`) toma **directo** `err.error?.detail ?? err.error?.title` de cualquier response de error y lo relanza como el mensaje visible al usuario — **cualquier excepción de negocio nueva que se agregue en el backend, si no está en español, se filtra tal cual a la UI**. Se tradujeron todos los mensajes existentes (2026-09-09). Regla para el futuro: **toda excepción nueva en `services/exception/` debe tener su mensaje en español desde que se escribe**, no hay capa intermedia que lo traduzca.
  **Gotcha aparte descubierto al debuggear esto:** el `errorInterceptor` transforma el `HttpErrorResponse` en un `Error` plano (`throwError(() => new Error(message))`) — en el `.subscribe({ error: (err) => ... })` de cualquier componente, `err` ya NO es un `HttpErrorResponse` (no tiene `.status`, `.error`, etc.), es un `Error` normal con `.message` ya resuelto por el interceptor. Cualquier código que intente chequear `err.status === 409` en un componente nunca va a matchear — hay que usar `err.message` directamente.

- (2026-09-09) **Confirmado en producción con destinatario real (no de prueba): el email de invitación llega bien desde `notificaciones@mygym.cl`** tras verificar el dominio en Resend — probado agregando un email real (`djchalo@gmail.com`) como admin de Fortis, el usuario confirmó recepción. Cierra del todo la feature de invitación por email.
- (2026-09-09) **Patrón nuevo: `ToastController` de Ionic para confirmar acciones exitosas.** El usuario probó agregar un admin real y no vio ninguna confirmación visible ("no me dijo nada") — mismo tipo de bug que el de la sección anterior (feedback ausente/mal ubicado), pero esta vez en el camino feliz (success), no en el de error. Se agregó un toast (`toastController.create({ message, duration: 4000, position: 'bottom', color: 'success' })`) en `submitAdmin()` de `gym-form.ts` y en `submitMember()` de `gym-admin.ts` — primer uso de `ToastController` en el proyecto, no requiere agregar `IonToast` al array `imports` del componente (se crea programáticamente vía overlay, no es una directiva de template). **Patrón a replicar**: cualquier acción mutante (crear/agregar/guardar) que no tenga una señal visual obvia e inmediata (la fila apareciendo en una lista no siempre alcanza, sobre todo si hay un efecto secundario invisible como "se mandó un email") debería confirmar con un toast, no dejar que el usuario adivine si funcionó.
- (2026-09-09) **Gotcha de testing con Playwright: nunca usar `waitForTimeout` fijo para confirmar que una mutación (crear/editar/eliminar) terminó — esperar la respuesta de red real.** Un barrido de QA completo (crear/editar/eliminar bloques, activar/desactivar admin, agregar socio) dio 8 fallas falsas en la primera corrida porque las aserciones se ejecutaban antes de que la respuesta del POST/PUT/DELETE volviera y el signal de Angular se actualizara — con `page.waitForResponse(res => res.url().includes('/blocks') && ...)` en vez de `page.waitForTimeout(800)`, todas las mismas aserciones pasaron limpio. Lección: un fallo de test que aparece solo en acciones que disparan un request no confirmado explícitamente es casi siempre timing del test, no del producto — confirmarlo con `waitForResponse` antes de reportar un bug real.
- (2026-09-09) **Gotcha de Playwright + `ion-modal`: no encadenar `.locator()` desde un locator de `ion-modal`.** El contenido del modal (vía `<ng-template>`) no es alcanzable haciendo `page.locator('ion-modal').locator(...)` — probablemente por cómo Ionic proyecta el contenido a través de shadow DOM. Consultar los inputs/botones directamente a nivel de página (`page.locator('ion-input[placeholder="..."] input')`) funciona bien, ya que Playwright sí atraviesa shadow DOM para selectores CSS simples cuando no se los ancla a un locator padre problemático.

- (2026-09-12) **Personalización del panel de gym-admin ampliada: temas completos (fondo+superficie+botón) y subida real de logo (imagen).**
  - **Paletas → temas.** `GymPalette.java` (backend) y `PALETTES` (frontend, `gym-admin.ts`) pasaron de 7 a 12 combinaciones — mismo patrón de fuente-de-verdad duplicada, actualizar ambas listas si se agrega/cambia un color. Cada tema sigue siendo solo `{key, hex, contrast}` — el fondo y la superficie **NO** se hardcodean por tema: se derivan en el momento con `deriveSurfaceTint(accentHex)` (conversión hex→hue, luego HSL fijo en `s=22 l=15` para el fondo y `s=22 l=19` para la card) en `gym-admin.ts`. Esto da un fondo distinto por gimnasio sin tener que autorizar 12 pares de hex a mano, y matemáticamente cae cerca del ink de marca original (`#1e2c30` es ~H200 S23 L15) — cada gym se siente una variación del mismo producto, no un esquema random. Los `--gym-panel-bg`/`--gym-panel-card` resultantes se bindean en `gym-admin.html` (header + content) y se consumen en `gym-admin.scss` vía `ion-content { --background: var(--gym-panel-bg, ...) }` / `ion-card { --background: var(--gym-panel-card, ...) }` — el header/toolbar de navegación se mantiene con el color de marca fijo a propósito (solo el área de contenido personaliza), matching patrones comunes de SaaS (chrome de nav consistente, workspace personalizable).
  - **Selección de tema en UI:** las tarjetas de paleta pasaron de círculos de un solo color a mini-preview de dos capas (fondo derivado + un "botón" de muestra en el acento) para que el admin vea el combo completo antes de elegir, no solo el acento suelto — sigue el patrón "preview en vivo" que recomendó la skill `ui-ux-pro-max` para este caso. Se mantiene el muestreo aleatorio de N de 12 + "Ver otras opciones" (ahora N=4, antes N=3 de 7) para no abrumar con las 12 opciones a la vez.
  - **Subida de logo real:** nuevo endpoint `PUT /api/gym-admin/gym/logo` (`GymLogoUpdateRequest`, campo único `logo` de hasta 400.000 caracteres). Acepta DOS formatos en el mismo campo de texto (reutiliza la columna `logo_svg` existente, sin migración): SVG crudo (sanitizado con la misma lista de patrones prohibidos que ya usaba `BrandingSuggestionService`, ahora extraída a `SvgSanitizer.java` compartido) o una imagen raster como `data:image/{png,jpeg,webp};base64,...`. El resize/recorte a 256×256 (contain, sin recortar, fondo transparente) se hace **en el cliente** con un `<canvas>` antes de subir — nunca se manda la imagen original sin comprimir. El frontend decide cómo renderizar según el prefijo del string guardado: `data:image/` → `<img [src]>`, `<svg` → `[innerHTML]` (como antes).
  - **Gotcha de Vercel CLI:** `npx vercel@latest` saltó de 59.14.0 a 59.16.0 en medio de esta sesión y esa versión nueva reportó `"Logged out"` (`whoami` sin credenciales) pese a que el login seguía válido — pinchar una versión conocida-buena (`npx vercel@59.14.0`) resolvió el deploy sin re-loguearse. Si un deploy de golpe pide login sin que nadie haya cerrado sesión, sospechar primero de un salto de versión de `@latest`, no de un problema de auth real.

- (2026-09-12) **Generador de bloques en serie** (`bloque-series-modal`, compartido entre super-admin y gym-admin, igual que `bloque-form-modal`): el admin marca días con checkbox, define hora de apertura/cierre y duración de cada bloque (30/45/60/90/120 min, `ion-select`), y el componente arma un **draft en el cliente** (sin pegarle al backend todavía) — un bloque por cada tramo de `duración` minutos que entra completo entre apertura y cierre, repetido por cada día marcado; el admin puede quitar entradas sueltas del draft antes de confirmar. Al confirmar, el padre (`gym-form.ts`/`gym-admin.ts`) llama al endpoint de **crear un bloque ya existente** (`createBlock`/`createMyBlock`) **una vez por entrada, en secuencia** (`firstValueFrom` + `for...of` con `await`) — no se creó un endpoint de bulk-create nuevo, se reusa el de a uno.
  **Confirmado (no bug):** al probar esto con Playwright, el conteo final de bloques visibles en pantalla dio 0 justo después de confirmar 14 bloques, aunque el toast decía "Se crearon 14" — un `fetch` directo a la API mostró que los 14 sí se habían guardado bien, y una recarga de página los mostró todos correctamente. Es el mismo patrón ya documentado (no confiar en un `waitForTimeout` fijo para una operación de red) llevado a su caso más extremo: acá son **14 llamadas HTTP secuenciales**, fácilmente más lentas que cualquier espera fija razonable en un test. Si un test de una acción "en lote" da un conteo final en 0 pero el toast de éxito sí apareció, verificar primero contra la API directa antes de asumir que el frontend no refrescó la lista.
- (2026-09-12) **Gotcha de Vercel CLI:** ver entrada anterior sobre `vercel@latest` saltando de versión y perdiendo credenciales — sigue vigente, se repitió el mismo día (pinchar `vercel@59.14.0` para deployar).

## Cómo mantener esta skill

Cada vez que resolvamos algo no trivial, tomemos una decisión de arquitectura, o descubramos una convención o gotcha del proyecto: agregar una entrada corta (qué + por qué) en la sección que corresponda. Fecha las entradas si el contexto puede volverse obsoleto.
